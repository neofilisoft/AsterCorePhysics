# Bridge Demo

This sample demonstrates the intended integration flow:
- add one `AsterCoreWorldBehaviour` to the scene
- add `AsterCoreBodyBehaviour` to dynamic or static GameObjects
- remove Unity Rigidbody components from AsterCore-driven objects
- press Play and let AsterCore drive the transforms
