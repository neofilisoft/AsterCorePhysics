# Unity Integration

The Unity middleware package lives in `Integrations/Unity/AsterCorePhysics`.

Use it as a local Unity package and copy `AsterCoreCAPI.dll` into `Runtime/Plugins/Windows/x86_64/` before entering Play mode.
